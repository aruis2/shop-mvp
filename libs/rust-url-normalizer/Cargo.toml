[package]
name = "rust-url-normalizer"
version = "0.1.0"
edition = "2024"

[dependencies]
axum = "0.8"
tower = "0.5"
http = "1.0"

[dev-dependencies]
tokio = { version = "1", features = ["macros", "rt"] }
