[package]
name = "rust-ratelimit"
version = "0.1.0"
edition.workspace = true
description = "🚦 HN-style in-memory sliding-window rate limiter — zero external deps"
license = "MIT OR Apache-2.0"

[dependencies]
tracing.workspace = true
