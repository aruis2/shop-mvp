[package]
name = "storage"
version = "0.1.0"
edition = "2024"
description = "Universal file storage trait (local / GCS / S3)"
license = "MIT OR Apache-2.0"

[dependencies]
async-trait.workspace = true
thiserror.workspace = true
tokio.workspace = true
uuid.workspace = true
